# Development overrides — not committed to production deployments
services:
  app:
    environment:
      XDEBUG_MODE: develop,debug
      XDEBUG_CONFIG: "client_host=host-gateway client_port=9003 idekey=PHPSTORM"
